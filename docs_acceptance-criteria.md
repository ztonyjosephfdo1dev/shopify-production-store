# Acceptance Criteria

This file enumerates the acceptance criteria for feature releases and deployments.

- All production changes are represented by a commit and pull request (if using branches).
- Each deployment must have:
  - Backup of live theme (duplicate theme)
  - Successful preview of changes on duplicate theme
- Changelog entry added for each release
- All secrets are stored outside the repository (GitHub Secrets)